/**
 * faultline dashboard — research visualization interface.
 *
 * Pages (Day 35–40):
 *   1. Experiments — overview with headline metrics
 *   2. Comparison — charts with confidence intervals
 *   3. Failures — failure taxonomy explorer
 *   4. Trace viewer — step-by-step execution trace
 *   5. Run configuration — settings and export
 */

export default function App() {
  return (
    <div style={{ padding: "2rem", fontFamily: "system-ui, sans-serif" }}>
      <h1>faultline</h1>
      <p>Dashboard not yet implemented — see Phase 6 (Days 35–40).</p>
    </div>
  );
}
