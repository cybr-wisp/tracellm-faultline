# Smoke test — minimal experiment to verify the pipeline runs end-to-end.
experiment_id: smoke-001
description: "Minimal smoke test with fake provider and one task."

tasks:
  - order-001

strategies:
  - baseline

corruption_modes:
  - clean
  - explicit_error

providers:
  - fake

trials: 1
concurrency: 2
timeout_seconds: 30.0
